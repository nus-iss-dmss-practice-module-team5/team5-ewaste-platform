export const LOGIN_PATH = "/login";
export const HOME_PATH = "/home";
export const EXPIRED_LOGIN_PATH = "/login?expired=1";

/** Refresh the access JWT this many ms before it expires. */
export const ACCESS_REFRESH_SKEW_MS = 5_000;
